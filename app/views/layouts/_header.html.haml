%header{:class => 'masthead', :role => 'banner'}
	.row
		.column.column--1-of-1
			%nav
				= link_to "Home", root_path
				- if user_signed_in?
					= link_to 'Gallery', uploads_path
					= link_to 'Write a story', new_post_path
					= link_to 'Log Out', destroy_user_session_path, method: :delete
